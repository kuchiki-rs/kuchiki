[package]
name = "kuchiki"
version = "0.2.1"
authors = ["Simon Sapin <simon.sapin@exyr.org>"]
license = "MIT"
description = "(朽木) HTML/XML tree manipulation library"
repository = "https://github.com/SimonSapin/kuchiki"

[lib]
name = "kuchiki"
doctest = false

[features]
unstable = [
    "string_cache/unstable",
    "rc/unstable",
    "html5ever/unstable",
    "selectors/unstable",
]
with-hyper = ["html5ever/hyper"]

[dependencies]
matches = "0.1.2"
html5ever = "0.3"
string_cache = "0.2"
selectors = "0.2.0"
rc = "0.1.0"

[dev-dependencies]
tempdir = "0.3"
html5ever = {version = ">=0", features = ["hyper"]}
